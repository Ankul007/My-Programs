#include <iostream>

using namespace std;

int main(){
    int i,j,k,n;//124992
    int bird[1000000];
    int a=1,b=2,c=3,d=4,e=5;
    int a1=0,b1=0,c1=0,d1=0,e1=0;
    
        cin >> n;
        
        for(i=0;i<n;i++)
        { 
           cin>>bird[i];
           if(bird[i]==a)     
                a1++;
           else if(bird[i]==b)     
                b1++;
           else if(bird[i]==c)     
                c1++;
           else if(bird[i]==d)     
                d1++;
           else if(bird[i]==e)     
                e1++;
           
        }
       if(a1 >= b1 && a1>=c1 && a1>=d1 && a1>=e1)
       {       
               cout<<"1";
       }
        else if(b1 >= a1 && b1>=c1 && b1>=d1 && b1>=e1)
       { 
               cout<<"2";
       }
                else if(c1 >= b1 && c1>=a1 && c1>=d1 && c1>=e1)
       { 
               cout<<"3";
       }
                else if(d1 >= b1 && a1>=c1 && d1>=a1 && d1>=e1)
       { 
               cout<<"4";
       }
                else if(e1 >= b1 && e1>=c1 && e1>=d1 && e1>=a1)
       { 
               cout<<"5";
       }
        
        
        
        
    
}
